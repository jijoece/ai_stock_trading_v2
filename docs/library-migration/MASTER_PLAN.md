# Master Plan — Revised PR Sequence

This supersedes the literal PR sequence in `docs/milestones/rebuild/plan.md`
where repository evidence and accepted ADRs required adjustment. See
`DECISIONS.md` for the reasoning behind each divergence and
`DEPENDENCY_MATRIX.md` for the compatibility findings that shaped PR 1, 4,
and the Category-B PRs (13/14).

Per-PR execution still follows `plan.md`'s "Per-PR execution protocol"
(bounded context read, verify current state, present implementation plan,
clear planning context, implement, test incrementally, review, update
status, commit and stop). This document defines *what* each PR contains;
it does not re-litigate *how* each PR session should run.

| PR | Title | Scope | Dependency on earlier PRs | Risk | Model |
|---|---|---|---|---|---|
| 0 | Architecture decisions and ADR reconciliation | This document set; no code | None | Low | Opus plan |
| 1 | Dependency foundation and planning corrections | `pyproject.toml` + `paper_runtime/pyproject.toml`: add `exchange_calendars` as a base dependency, `indicators`/`analytics`/`observability` extras, `hypothesis`/`time-machine` into `dev`, bump LumiBot to `4.5.78` in `paper_runtime`; **remove the root `paper` extra** (unresolvable `jsonschema` conflict — `DECISIONS.md` D5) so `paper_runtime/pyproject.toml` is the sole LumiBot dependency declaration; Python floor stays `>=3.10` (VectorBT floor requirement does not apply — not added this PR); VectorBT and Riskfolio-Lib deferred, not added; CI gains blocking jobs for each new extra and a Python-3.10 floor job. See `DEPENDENCY_MATRIX.md` Section 6 for the full correction record | PR 0 | Low | Sonnet |
| 2 | Boundary validation evaluation | Pydantic v2 at YAML/env/CLI/JSONL boundaries only (`DECISIONS.md` D2); frozen domain dataclasses unchanged; compare against current hand-written validation | PR 1 | Medium | Sonnet, Opus review |
| 3 | `exchange_calendars` migration | Replace `evaluation/market_calendar.py`; fixture parity across sessions/holidays/DST/pre-market/intraday/after-hours/previous-next session/session counts | PR 1 | Low-Medium | Sonnet |
| 4 | Generic indicator parity and migration | Replace `scripts/indicators.py` with TA-Lib; require a supported binary wheel first and verify wheel resolution on Linux CI and supported macOS architectures; use a system package/source install only as an explicitly documented fallback if a target platform lacks a compatible wheel (see `DEPENDENCY_MATRIX.md` Section 6 — corrected 2026-07-26, PR 1); document warm-up/null/rounding semantics; pandas-ta-classic evaluated only as fallback if wheel resolution actually fails somewhere | PR 1 | Low-Medium | Sonnet |
| 5 | Vectorized research library selection and adapter | Evaluate an OSI-approved vectorized-research library, or obtain explicit owner approval for VectorBT's Apache-2.0 + Commons Clause terms (`DECISIONS.md` D4), before adding any dependency; then build a new, additive `research` group adapter for signal matrices/parameter sweeps; add an import-boundary test (the selected library never imported from `paper_runtime` or vice versa, analogous to the LumiBot AST test); no execution authority | PR 1 | Low | Sonnet |
| — | **Pre-step before PR 6** | Resolve the LumiBot-backtest-mode dependency/process-boundary question in `DECISIONS.md` D4 open item 1. **COMPLETE (2026-08-01):** Opus review, pinned-version feasibility spike, and sentinel-`.env` suppression proof all done (`pre-step-06/EVALUATION.md`, `dotenv_sentinel_output.txt`); the owner accepted ADR 0009 and selected `backtest_runtime/` (Option B). A first pass under Sonnet reached a different conclusion and is withdrawn | PR 5 | — | Opus review |
| 6 | LumiBot backtest evaluation adapter | **MERGED** (`bbd7a1f`, PR #18; branch `migration/06-lumibot-backtest-adapter`; see `STATUS.md` "Completed work (PR 6)"). New adapter beside existing `backtesting/engine.py`, in the boundary ADR 0009 defines (`backtest_runtime/`, its own distribution — **not** a second in-process import site); no deletion. **Deliverables, all merge conditions rather than preconditions (ADR 0009 Decision 4):** create `backtest_runtime/`; an installable `backtest_runtime/pyproject.toml` pinning `lumibot==4.5.78` with an explicit `requires-python`; the distribution's own tests, with no `importorskip` guard; and a **blocking** `backtest-runtime-tests` CI job asserting `pip check`, the exact LumiBot version, and the five credential-safety properties of ADR 0009 Decision 2 (no credential value available or loaded; nothing loaded from the environment or a `.env`/`.env.local`; no broker or live data provider initialized; zero outbound attempts; determinism) — **not** zero credential reads. Also repairs the AST import-boundary test, which currently skips in CI | Pre-step above (complete) | Medium | Sonnet, Opus review |
| 7 | Backtest parity report | **MERGED** (`5b9e1e3`, PR #19; branch `migration/07-backtest-parity-report`; report at `docs/library-migration/pr7/PARITY_REPORT.md`, raw data under `pr7/results/`). Ran a six-case checked-in fixture set through both engines in their two separate environments (ADR 0009 Decision 5) and compared orders, fills, entry/exit timestamps, prices, quantities, cash, positions, fees, realized/unrealized P&L, equity, drawdown and final value. Fill timing is read from LumiBot's **authoritative broker trade-event log**, never inferred from which bar's open matches a fill price. Comparison method: **bounded Option B** (`DECISIONS.md` D6) — reference strategy **v2** adds one control, `strategy.entry_after_session`, and nothing else, because LumiBot cannot book before its second bar while the legacy engine cannot enter before its third, so no fixture (including one with deliberately matching consecutive opens) can produce an identical case under Option A. `case_f_exact_entry_parity` is that case: both engines book the entry 2024-01-04 at 101.0 for 10 shares and agree on every economic number and on **every co-dated session with none excluded**, the comparator asserting fifteen dimensions. Every difference classified and enforced by the comparator: 13 old-engine defect (`BacktestResult` carries no order or position records; **run identity ignores the dataset**, so two runs over different bars collide onto one `backtest_run_id` and `_persist_result` silently discards the second), 14 adapter defect (0 behavior, 14 capability — no fees, slippage, realized P&L or exit support), 93 intentional library semantic difference, 0 unsupported requirement. The two former behavior defects (a fill's `market_date` and the entry session's daily state reported one session late) were reclassified as LumiBot execution-loop semantics and fixed in reference strategy v2, which also bumps the result schema to `backtest_runtime.result.v2`. The legacy engine was not changed — PR 8 decides | PR 6 | Medium | Sonnet |
| 8 | Decide whether custom backtest component can be safely removed | **DECIDED, IMPLEMENTED, MERGED** (PR #20, branch `migration/08-backtest-removal-decision`, based on `5b9e1e3`; record at `docs/library-migration/pr8/DECISION.md`, `DECISIONS.md` D7). Decision gate only — documentation, no code. **Outcome: not approved for removal.** `backtesting/engine.py`/`models.py` stay authoritative indefinitely and move to `PRESERVATION_MANIFEST.md`; `REMOVAL_MANIFEST.md`'s conditionally-eligible row is closed, so no unresolved backtest target reaches PR 17 or PR 18. `backtest_runtime/` is **kept** as an additional non-replacing offline cross-check (ADR 0009's delete-the-distribution rollback is not taken), with a maintenance review trigger. Decisive reasons are not the parity numbers — the two engines agree on the exact case — but the capability gap plus two non-feature properties: `Decimal` versus `float` accounting, and the engine's shared `calculate_partial_close_quantity` with `paper_books/lifecycle_state.py`, which ADR 0009's boundary forbids the runtime from importing. **Revised 2026-08-02 (review round):** a third reason — a point-in-time availability axis "enforced at three layers" — was **withdrawn as inaccurate**; the engine applies one run-level `as_of` cutoff, not per-session knowability (D7 ruling 5, `pr8/DECISION.md` §4.3), and the verdict is re-derived without it. PR 7's D13 + D1 (peak-equity seeding, state-series start) is unresolved **by design** and recorded as a precondition on any future replacement proposal | PR 7 | High (decision) | Opus review |
| 8a | Legacy backtest run identity, order records, and per-session bar availability | **Follow-up created by PR 8, not started.** Three defects in the component PR 8 chose to keep, all behavior changes to `backtesting/engine.py` and therefore out of a decision-only PR: (a) **run identity ignores the bar dataset** — `_configuration_hash`/`input_hash` exclude the bars, so two runs over provably different data share one `backtest_run_id` and `_persist_result` discards the second as an idempotent replay while the collision guard never fires; bind a canonical bar-dataset digest into `input_hash` and decide the stance on already-stored rows (a `code_version` bump already participates in the hash). (b) **`backtest_orders` is created and never written** — the table exists in `storage/paper_books_schema.py` with a `rejection_reason` column while the engine's 11 rejection reasons live only in the in-memory result; either persist orders and rejections or delete the table and record that order-level history is deliberately not retained. (c) **Bar availability is enforced once per run, not per session** — `run_backtest` passes a single `as_of` of `end_date 23:59:59 UTC` (`engine.py:140-149`), the provider filters against only that value (`data_provider.py:25-30`), `HistoricalBar` merely trusts a caller-set `point_in_time_safe` flag (`models.py:26-30`), and `strategy_signal_to_entry_signal` reduces `data_as_of` to a date (`strategies/backtest_adapter.py:44`), so a bar available after a signal/session but before the run's end is usable in that earlier simulated period; thread a per-session/per-signal `as_of` through the simulation, keep `data_as_of` at timestamp resolution, decide whether `point_in_time_safe` stays caller-asserted, and add a test that a late-`available_at` bar is excluded from the earlier session. Expect fixture and test churn — the existing 23 engine/adapter tests encode the current run-level semantics. See `pr8/DECISION.md` §8 | PR 8 | Medium | Sonnet, Opus review |
| 9 | Strengthen LumiBot runtime normalization contract | **IMPLEMENTED** (branch `migration/09-lumibot-normalization-contract`, based on `b57f891`; record in `STATUS.md` "Completed work (PR 9)", `DECISIONS.md` D8). One normalization contract, declared once per distribution (ADR 0002/0009 forbid a shared package) and kept identical by an AST drift test: `src/trading_research/runtime/normalization.py` and `paper_runtime/.../normalization.py`. Normalizes orders/statuses/fills/positions/account snapshots at both boundaries, fail-closed throughout. Closes a defect where the gateway could emit `EXPIRED`/`CANCEL_REQUESTED`, absent from `execution/broker_snapshots.py::SUBMISSION_STATES`, writing broker-submission rows that could never be read back. `paper_books/external_broker.py`'s state machine is **not** changed — its coverage is pinned by test, with `ERROR` deliberately unmapped (`DECISIONS.md` D8 ruling 4). **Follow-up (D8 rulings 6-9):** the broker-status polling path (`services/sync_paper_orders.py`) now validates against `BROKER_REPORTABLE_STATUSES` instead of the narrower `EVENT_TYPES`, so it no longer crashes on `CANCEL_REQUESTED`/`EXPIRED`; `RuntimeClient` (`runtime/client/process_client.py`) now calls the normalization parsers on every response instead of returning raw dicts; a shared declarative accept/reject corpus (`tests/fixtures/normalization_corpus.json`) is run independently against both distributions as semantic drift protection beyond constant/name equality; and the remaining silent repairs in `lumibot_gateway.py` (a missing `filled_qty` defaulted to `0`, a missing `submitted_at`/`updated_at` fabricated from the clock, a missing account `currency` defaulted to `"USD"`) are closed. **Follow-up 2 (D8 rulings 10-12):** `RuntimeOrderSnapshot` now rejects a non-positive `quantity`, canonicalizes `submitted_at`/`updated_at`, and preserves `book_id`/`symbol`/`side`/`limit_price`/`time_in_force`/`account_fingerprint` instead of dropping them; the Milestone 11 external-order methods (`get_order_by_client_order_id`, `get_order_by_broker_order_id`, `cancel_external_order`, `list_order_fills`, `get_external_positions`, `get_external_account_snapshot`, `list_recent_external_orders`) now validate through new `ExternalOrderSnapshot`/`ExternalFillSnapshot`/`ExternalPositionsSnapshot`/`ExternalAccountSnapshot` parsers instead of returning raw dicts; and the `time_in_force` DAY default in `lumibot_gateway.py` was narrowed to apply only to a `client_order_id` inside this project's own id namespace (superseded by follow-up 3 below). **Follow-up 3 (D8 rulings 13-14):** `submit_limit_order` now validates its response through `ExternalOrderSnapshot` (no-retry behavior unchanged); `get_order_by_client_order_id`/`get_order_by_broker_order_id` now validate the `{"found": ...}` envelope itself (`parse_client_order_lookup_response`/`parse_broker_order_lookup_response`) rather than trusting it, closing a gap where a malformed envelope could be read as an authoritative broker NOT_FOUND capable of unlocking a retry; and follow-up 2's `client_order_id`-namespace-prefix ownership check for the `time_in_force` default was found insufficient (a prefix is broker-echoed data, not proof of ownership) and removed — an absent `time_in_force` now always fails closed, with no default. **Replaces original plan.md PR 9** — see `DECISIONS.md` D1 | PR 1 | High | Opus plan + Sonnet |
| 10 | Broker-to-paper_books reconciliation parity tests | **IMPLEMENTED** (branch `migration/10-broker-paper-books-reconciliation-parity`; record in `STATUS.md` "Completed work (PR 10)"). New test file `tests/unit/test_external_broker_runtime_client_parity.py` proves that a real `RuntimeClient` (the only production type structurally satisfying `ExternalPaperRuntime`), driven by scripted, PR-9-normalized wire responses, reconciles correctly into the real `paper_books` cash ledger and positions tables — closing a gap where every existing reconciliation test used a hand-rolled double that bypassed the normalization contract entirely. Covers a matched fill, a cash mismatch, a position mismatch, the PR 9 motivating `EXPIRED`-status defect round-tripping cleanly, and a malformed broker response failing closed without corrupting the book. **The book ledger was not removed** — no file under `src/`, `scripts/`, `paper_runtime/src/`, or `config/` was modified. **Replaces original plan.md PR 10** — see `DECISIONS.md` D1 | PR 9 | High | Opus plan + Sonnet |
| 11 | QuantStats/analytics migration | Replace `evaluation/metrics.py` formulas with `quantstats-lumi` + `empyrical-reloaded`; keep `Decimal`→float boundary explicit; annualization/insufficient-data semantics must match | PR 1 | Low-Medium | Sonnet |
| 12 | Riskfolio-Lib evaluation only | Advisory allocation output, gated through the same "advisory only, never authoritative" boundary as the Claude research overlay (ADR 0003 pattern); document that this group transitively pulls VectorBT's pandas/numpy chain | PR 1 | Medium | Sonnet, Opus review |
| 13 | SQLAlchemy/Alembic feasibility and ADR | No implementation. Must explicitly test: (a) trigger-protected tables (append-only tables, `real_orders`) only ever touched via SQLAlchemy Core statements, never ORM-session flush/unit-of-work; (b) whether Alembic's branching revision graph can be constrained to linear-only history matching the current monotonic gate. Produce a new ADR only if adoption is recommended | PR 1 | High (decision) | Opus review |
| 14 | APScheduler/Tenacity feasibility | Reframed from "replace" to "coexist": APScheduler v3 (not v4 — not production-ready) may handle simple due-time triggering only; existing lease/generation-fencing logic stays custom. Tenacity gains a structural test proving no `@retry`/`Retrying()` usage wraps the ambiguous-broker-retry path in `external_broker.py` | PR 1 | High (decision) | Opus review |
| 15 | Structlog migration | Replace `logging_config.py`; port custom redaction/secret-registration logic to a structlog processor; parity test for redaction behavior | PR 1 | Medium | Sonnet |
| 16 | OpenTelemetry migration | Additive tracing/metrics SDK only; no exporter configured by default in tests (offline-safe); domain telemetry (`cycle_telemetry.py`, `paper_books/metrics.py`) is unaffected and stays | PR 15 | Low-Medium | Sonnet |
| 17 | Remove only approved commodity implementations | Execute only removal-manifest entries that passed parity in PR 3/4/11/15. **PR 8's conditional contribution is resolved: it approved no removal** (`DECISIONS.md` D7), so nothing under `backtesting/` is in this PR's scope and `REMOVAL_MANIFEST.md` carries no unresolved backtest target | PR 3, 4, 11, 15 (PR 8 resolved — contributes nothing) | Medium | Sonnet, Opus review |
| 18 | Final authority and safety audit | Confirm plan.md completion criteria; no dual authorities remain; `REMOVAL_MANIFEST.md` has no unresolved target | All prior | High | Opus review |

## Explicit non-goals carried forward from plan.md and docs/milestones/rebuild/1.md

- No PR in this sequence removes `paper_books/*`, `backtesting/engine.py`,
  `external_broker.py`, or `storage/*` without an explicit decision gate
  (PR 8, PR 13) and, for `paper_books`, an approved superseding ADR that
  does not currently exist and is not scheduled. **`backtesting/engine.py`'s
  gate has now run and declined** (PR 8, 2026-08-02, `DECISIONS.md` D7): it is
  a preserved component, and proposing its removal again requires the full
  ADR-supersession process, not a fresh parity report.
- No PR moves a hard safety limit, account-fingerprint check, or
  ambiguous-retry rule into a library default, LumiBot strategy callback,
  broker configuration, CLI presentation logic, or environment variable.
- PR 2 (Pydantic) and PR 12 (Riskfolio-Lib) each require the same
  boundary-respecting pattern already established for Claude's research
  overlay: advisory/boundary only, never authoritative over risk, sizing, or
  order construction.
