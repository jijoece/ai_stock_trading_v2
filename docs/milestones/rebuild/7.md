Work only on PR #15.

The repository owner accepts ADR 0009 and selects Option B:
an isolated, credential-free backtest_runtime/ distribution.

Address the remaining PR #15 review findings:

1. Remove the circular pre-step gate
- Mark ADR 0009 as Accepted.
- Owner acceptance, the completed architecture review, and the successful
  feasibility spike complete the pre-step.
- Do not require backtest_runtime/ or its CI job to exist before PR 6 starts.
- Make creation of backtest_runtime/, its installable pyproject.toml,
  tests, and blocking CI job explicit PR 6 acceptance criteria.
- Mark PR 6 as unblocked but not started.

2. Correct the credential-safety requirement
- Do not require “zero credential reads,” because LumiBot reads credential
  variable names even when no values are available.
- Require proof that:
  - no broker credential values are available to or loaded by LumiBot;
  - no credentials are loaded from process environment or .env files;
  - no broker/live provider is initialized;
  - zero outbound network attempts occur;
  - repeated offline runs remain deterministic.
- Reconcile this wording everywhere, especially ADR 0009, STATUS.md,
  DECISIONS.md, MASTER_PLAN.md, EVALUATION.md, and the PR description.

3. Prove .env suppression
- Determine and document the exact import-time mechanism used to prevent
  the pinned LumiBot/python-dotenv versions from loading an operator’s .env.
- Extend the feasibility spike with a sentinel .env in the original working
  directory containing unique fake Alpaca credential values.
- Run the spike from that setup and prove:
  - sentinel values are never loaded or propagated;
  - zero outbound attempts occur;
  - the backtest uses only caller-supplied fixture data;
  - results remain deterministic.
- Use fake sentinel credentials only. Do not access or expose real credentials.
- Commit updated raw evidence or a concise reproducible result record.

4. Reconcile the complete documentation set
- ADR 0009 must be Accepted.
- DECISIONS.md D4 must be resolved.
- STATUS.md must show the pre-step complete and PR 6 unblocked/not started.
- MASTER_PLAN.md must make backtest_runtime/ and blocking CI PR 6 deliverables.
- EVALUATION.md must no longer list PR 6 implementation work as a prerequisite
  for starting PR 6.
- Preserve PR #16’s updates in overlapping files.
- Update the PR title and description to match the final accepted state.

Validation:
- Search the documentation for stale contradictions such as:
  “Proposed,” “still blocked,” “zero credential reads,” and statements that
  backtest_runtime/ must already exist before PR 6 starts.
- Run the updated sentinel-.env feasibility check.
- Run relevant repository tests after the rebase.
- Keep the change limited to PR #15’s decision record and feasibility evidence.

Do not create backtest_runtime/, begin PR 6 implementation, merge the PR,
or modify trading behavior.

Commit and push the corrected PR #15 branch. When finished, report:
- rebase/conflicts resolved;
- files changed;
- sentinel-.env evidence;
- test results;
- commit SHA pushed;
- whether PR #15 is now mergeable.