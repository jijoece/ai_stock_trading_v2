Implement PR 4: replace the custom EMA, RSI, MACD, TRIX, and Bollinger calculations in `scripts/indicators.py` with TA-Lib.

Begin only after the PR #10 follow-up correction is merged.

## Start

Branch from the latest `main`:

```text
migration/04-talib-indicators
```

Confirm the pull request base will be `main`.

Read:

```text
docs/library-migration/STATUS.md
docs/library-migration/MASTER_PLAN.md
docs/library-migration/COMPONENT_MATRIX.md
docs/library-migration/REMOVAL_MANIFEST.md
scripts/indicators.py
pyproject.toml
.github/workflows/ci.yml
```

Find every caller and test importing:

```text
ema_series
rsi_wilder
macd
trix
bollinger
compute
```

## Establish parity before replacement

Before editing the formulas, capture reviewed golden outputs from the current implementation for deterministic fixtures.

Cover:

```text
increasing prices
decreasing prices
flat prices
oscillating prices
short inputs below every warm-up threshold
exactly-at-threshold inputs
long realistic synthetic series
```

Record expected numeric values directly in fixtures. Do not retain the old implementation as a second runtime authority.

## Preserve the public contract

Preserve function names, signatures, return shapes, dictionary keys, and list lengths.

Explicitly preserve or intentionally document these semantics:

* EMA uses SMA seeding and `None` warm-up padding.
* RSI first valid index remains unchanged.
* The existing flat-series RSI behavior must be tested explicitly; do not accept TA-Lib’s behavior without comparison.
* MACD line availability before the signal line must remain compatible. Do not assume `talib.MACD()` exposes the same early values; use TA-Lib EMA primitives when necessary.
* MACD signal and histogram alignment must remain unchanged.
* TRIX warm-up, index alignment, percentage scaling, signal alignment, and zero-denominator behavior must be tested.
* Bollinger Bands use population standard deviation.
* `%B` remains `0.5` when the upper and lower bands are equal.
* Convert TA-Lib/NumPy `NaN` values to the existing `None` representation at the boundary.
* Do not round internal calculations. Preserve `_round()` as the CLI-output boundary.
* Preserve `_slope`, `bars_since_below_ema20`, warnings, and other project-specific derived logic.

TA-Lib must become the sole authority for the migrated indicator calculations. Do not keep custom fallback formulas.

Do not add `pandas-ta-classic` unless a required CI platform demonstrably cannot install the verified TA-Lib wheel.

## Dependency behavior

TA-Lib remains in the existing `indicators` optional group.

When the script is run without the extra installed, fail with a concise actionable message:

```text
Install the indicators extra: pip install -e ".[indicators]"
```

Do not silently use the old formulas.

Add or update a blocking CI job that:

```bash
pip install -e ".[indicators]"
pip check
pytest <indicator-focused-tests> -q --tb=short
```

The ordinary default test suite must also remain green without accidentally masking failures in the dedicated TA-Lib job.

## Scope

Modify only the indicator implementation, focused tests, required CI coverage, and migration documentation.

Do not change:

```text
trading limits
authorization rules
paper_books accounting
broker execution
scheduling
provider integrations
scoring thresholds
strategy definitions
```

Do not migrate the ATR/Decimal boundary in `analysis/indicators.py` unless it is already explicitly included in the tracked PR 4 scope and full parity tests are added. Otherwise leave it for a separate change.

## Documentation

Update:

```text
docs/library-migration/STATUS.md
docs/library-migration/COMPONENT_MATRIX.md
docs/library-migration/REMOVAL_MANIFEST.md
```

Record:

* TA-Lib as the sole authority;
* old formulas removed;
* warm-up, `None`/`NaN`, alignment, and rounding parity;
* any intentional semantic differences;
* focused and full-suite test results;
* wheel installation result;
* no fallback authority;
* an exact bounded prompt for PR 5.

## Validation

Run:

```bash
pytest <indicator-focused-tests> -q --tb=short
pytest tests/ -q --tb=short
```

Confirm:

```text
custom EMA formula removed
custom RSI formula removed
custom MACD formula removed
custom TRIX formula removed
custom Bollinger formula removed
TA-Lib is the sole calculation authority
all public outputs remain compatible
real provider calls: 0
broker calls: 0
paper orders: 0
live orders: 0
scheduler enabled: no
```

Open one PR titled:

```text
PR 4: Replace custom indicators with TA-Lib
```

Stop after opening the PR. Do not merge automatically or begin PR 5.
