Choose **Option 3 — Decide per responsibility**.

Do not follow the original migration plan literally where repository evidence or accepted ADRs show that the custom implementation serves project-specific safety, accounting, audit, or isolation requirements.

## Governing migration principle

Use this distinction:

```text
Replace generic commodity infrastructure with open-source libraries.

Preserve project-specific domain infrastructure when it encodes safety,
book isolation, accounting invariants, auditability, operator authorization,
or ambiguous-side-effect recovery that the proposed library does not provide.
```

Accepted ADRs are not immutable, but they must never be superseded implicitly.

Any change that contradicts an accepted ADR must:

1. identify the exact ADR decision being changed;
2. explain what repository evidence invalidates the original decision;
3. prove that the replacement satisfies every original requirement;
4. include parity, safety, migration, and failure-recovery tests;
5. add a new ADR explicitly superseding the previous ADR;
6. be approved before implementation.

Until those conditions are met, the accepted ADR remains authoritative.

---

# Decision for ADR 0006 — Preserve custom paper-book accounting

Do not replace the current `paper_books/` accounting subsystem with LumiBot.

ADR 0006 remains accepted and authoritative.

The custom subsystem is not merely a generic portfolio ledger. It provides application-specific requirements including:

```text
BASELINE and ENHANCED book isolation
book_id-scoped cash and positions
FIFO lots
cash reservations before BUY submission
share reservations before SELL submission
settled versus reserved versus available cash
point-in-time valuation
immutable fill and accounting evidence
external broker fill reconciliation
transactional cash/position/fill updates
account-fingerprint continuity
operator-controlled external submission
ambiguous-order recovery
```

The inventory shows that LumiBot manages portfolio state at its runtime/process or broker-account scope and does not natively provide the repository’s independent `book_id` accounting semantics.

Therefore:

```text
LumiBot remains authoritative only for the isolated broker/runtime boundary.

paper_books remains authoritative for:
- logical book accounting;
- local cash;
- lots;
- positions;
- reservations;
- book-level valuation;
- book-level reconciliation;
- experiment isolation.
```

Do not implement the original PR 9 or PR 10 as written.

Replace them with:

```text
PR 9 — Strengthen the LumiBot runtime adapter and reconciliation contract

PR 10 — Validate paper_books accounting invariants against broker/runtime
fixtures without replacing the book ledger
```

LumiBot may supply normalized broker:

```text
orders
statuses
fills
positions
account snapshots
```

The application must reconcile those observations into the existing authoritative book-scoped accounting model.

Do not allow LumiBot to mutate application book state directly.

Do not delete:

```text
paper_books/cash_ledger.py
paper_books/positions.py
book-scoped lot accounting
cash/share reservations
book valuation
external reconciliation state
```

unless a future approved ADR demonstrates exact replacement semantics.

---

# Decision for ADR 0001 — Preserve domain dataclasses; allow boundary validation

Do not replace every frozen dataclass with Pydantic models.

ADR 0001 remains authoritative for internal domain models:

```text
@dataclass(frozen=True)
explicit __post_init__ invariants
immutable deterministic domain values
minimal runtime dependency
```

However, evaluate Pydantic v2 for bounded external boundaries where it can remove substantial hand-written parsing and validation.

Permitted uses include:

```text
YAML/environment configuration loading
CLI request validation
external provider response parsing
broker/runtime JSONL message validation
API or serialized DTO validation
```

Internal domain code should continue receiving immutable frozen dataclasses.

Recommended boundary:

```text
untrusted dict / YAML / JSON
        ↓
Pydantic boundary model
        ↓
explicit conversion
        ↓
frozen domain dataclass
```

Do not pass Pydantic models throughout strategy, risk, accounting, or persistence code merely for uniformity.

Revise the original PR 2 into:

```text
PR 2 — Introduce boundary validation without replacing domain models
```

The PR should compare:

```text
current hand-written configuration validation
current JSON/schema validation
Pydantic boundary implementation
dependency and performance impact
error-message behavior
unknown-field rejection
secret-field handling
```

Adopt Pydantic only where it produces a clear reduction in custom boundary-validation code.

Add a new ADR documenting this limited adoption only if implemented.

Pandera may still be evaluated separately for DataFrame contracts.

---

# Hard safety layer — Preserve without semantic weakening

The following application-owned controls must remain authoritative regardless of framework migration:

```text
$50 maximum single trade
$150 maximum submitted paper notional per UTC day
10% maximum symbol allocation
no options
no margin
no shorting
whole shares only
LIMIT orders only
DAY time in force
paper environment verification
account fingerprint verification
explicit operator preview and submit
exact approval phrase
never auto-retry an ambiguous broker transmission
authoritative NOT_FOUND evidence before retry
attempt-specific retry limits
cash and share reservations
append-only event and reconciliation evidence
```

Library defaults are not proof that these controls are enforced.

Each safety rule must continue to be enforced at the application’s authoritative gateway immediately before any broker-side effect.

Do not move these rules exclusively into:

```text
LumiBot strategy callbacks
broker configuration
CLI presentation logic
environment variables
```

---

# Revised migration categories

## Category A — Migrate to open-source libraries

Proceed with detailed planning for:

```text
exchange calendar/session logic
generic technical indicators
performance statistics
analytics reports and tear sheets
vectorized parameter exploration
DataFrame validation
generic bounded retry mechanics
generic scheduling mechanics
structured logging and telemetry
```

Every replacement requires fixture parity before deleting the custom implementation.

## Category B — Evaluate with parity before deciding

Do not pre-commit to replacement for:

```text
event-driven backtesting
SQLAlchemy
Alembic
APScheduler persistence
Tenacity integration
Pydantic configuration
PyArrow/Parquet persistence
```

For each, produce:

```text
current requirements
library capabilities
semantic gaps
migration cost
dependency impact
test strategy
recommendation
```

No production replacement until approved.

## Category C — Preserve custom application authority

Do not plan removal of:

```text
paper_books accounting
book isolation
cash/share reservations
operator approval
external-paper state machine
account fingerprinting
ambiguous-submission reconciliation
token/cost budgeting
activation controls
evidence provenance
AI research validation
promotion/readiness evidence
```

These are project-specific domain and safety components.

---

# Revised target architecture

Use:

```text
exchange_calendars
    → exchange sessions and holidays

VectorBT / TA-Lib
    → generic indicators and research exploration

QuantStats / compatible analytics packages
    → standardized performance reporting

LumiBot isolated runtime
    → Alpaca paper-broker communication and optional independent
      event-driven backtest validation

Pydantic at selected boundaries
    → untrusted configuration and serialized payload validation

Frozen dataclasses
    → internal domain models

paper_books
    → authoritative application book accounting and isolation

external_broker safety gateway
    → authorization, limits, identity verification, state machine,
      ambiguity handling, and reconciliation
```

Do not create two authoritative portfolio-accounting systems.

The boundary must be:

```text
broker/runtime observation
        ↓
validated normalized event
        ↓
application reconciliation
        ↓
authoritative paper_books accounting
```

---

# Required changes to the migration plan

Rewrite the proposed PR sequence before implementation.

At minimum:

```text
PR 0 — Architecture decisions and ADR reconciliation

PR 1 — Dependency compatibility and optional groups

PR 2 — Boundary validation evaluation
        Keep frozen domain dataclasses

PR 3 — exchange_calendars migration

PR 4 — Generic indicator parity and migration

PR 5 — VectorBT research adapter

PR 6 — LumiBot backtest evaluation adapter

PR 7 — Backtest parity report

PR 8 — Decide whether any custom backtest component can be safely removed

PR 9 — Strengthen LumiBot runtime normalization contract

PR 10 — Broker-to-paper_books reconciliation parity tests
         Do not remove the custom book ledger

PR 11 — QuantStats/analytics migration

PR 12 — Riskfolio-Lib evaluation only

PR 13 — SQLAlchemy/Alembic feasibility and ADR
         No implementation until approved

PR 14 — APScheduler/Tenacity feasibility

PR 15 — Structlog/OpenTelemetry migration

PR 16 — Remove only custom commodity implementations whose replacements
         passed parity and were approved

PR 17 — Final authority and safety audit
```

---

# Required planning output

Update the current plan to include three columns for every component:

| Component | Classification                          | Decision                      |
| --------- | --------------------------------------- | ----------------------------- |
| Example   | Commodity / Evaluated / Domain-specific | Replace / Evaluate / Preserve |

Also add:

```text
accepted ADR
whether migration conflicts with it
whether a superseding ADR is required
exact invariant the current implementation protects
whether the proposed library provides that invariant
```

Create a revised `REMOVAL_MANIFEST.md` containing only components approved for removal.

Do not list `paper_books` accounting files as removal targets.

Create a `PRESERVATION_MANIFEST.md` containing:

```text
domain-specific component
reason it remains custom
invariants protected
library boundary around it
tests proving the boundary
```

---

# Immediate next action

Remain in planning mode.

Do not edit production code.

Revise the architecture and PR plan using these decisions.

Present:

1. revised authority matrix;
2. revised PR sequence;
3. revised ADR impact table;
4. removal manifest;
5. preservation manifest;
6. risks that remain unresolved;
7. a compact prompt for PR 0.

Wait for approval before creating or editing migration documents.
